{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the main window. The backend sidecars are spawned from Rust, so no shell permission is exposed to the webview.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
